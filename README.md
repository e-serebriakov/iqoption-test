# iqoption-test

## instructions

#### development
* start webpack-dev-server `npm run start:dev`
* open `http://localhost:8080/`
* linting `npm run jslint` (optional)

#### production
```
    npm run build:prod
```